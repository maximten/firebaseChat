# Firebase based console chat :fire::fire::fire:

In order to run chat copy ```index.example.js``` in ```config``` directory to ```index.js``` and paste your firebase config options.  
Then run ```node index.js```
